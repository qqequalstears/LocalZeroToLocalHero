package Client.Model;

public enum GUIEvents {
    // Calls going out from the GUI
    LOGIN,
    REGISTER,
    NEWSTAGE,
    NOTIFYUSER,

    // Calls going into the GUI
    SUCCESSLOGIN,
    BADLOGIN;
}
